name: Firmware Image Build

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      name: Build firmware in docker container
      runs:
        using: 'docker'
        image: 'Dockerfile'
